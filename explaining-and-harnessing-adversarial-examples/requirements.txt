# Reproduction of "Explaining and Harnessing Adversarial Examples"
# (Goodfellow, Shlens, Szegedy; ICLR 2015, arXiv:1412.6572v3).
#
# Method dependencies (direct):
#   torch  — every model (softmax/logistic/maxout/RBF nets) and the FGSM
#            attack are implemented in PyTorch so that the input gradient
#            ∇ₓJ(θ,x,y) needed by η = ε·sign(∇ₓJ) comes from autodiff.
#   numpy  — array utilities / IDX MNIST loading.
# Test runner: pytest.
#
# CPU-only: the paper's MNIST experiments run on CPU here; torch is the +cpu
# wheel. The full transitive closure is pinned below for reproducibility
# (every package `uv pip freeze` reports for this exact install).

torch==2.7.1
numpy==2.3.2
pytest==8.4.2

# --- transitive dependencies (pinned to the resolved versions) ---
filelock==3.32.0
fsspec==2026.7.0
iniconfig==2.3.0
jinja2==3.1.6
markupsafe==3.0.3
mpmath==1.3.0
networkx==3.6.1
packaging==26.2
pluggy==1.6.0
pygments==2.20.0
setuptools==83.0.0
sympy==1.14.0
typing-extensions==4.16.0
