# virtual environments
.venv/
venv/
env/

# python build artifacts
__pycache__/
*.pyc
*.pyo
*.egg-info/
.pytest_cache/
build/
dist/

# runtime data: ACS PUMS csv files are downloaded by folktables (regenerable),
# so the raw census dumps stay out of git.  RESULTS are committed (a number
# whose evidence file is gitignored is a claim with its evidence deleted).
data/
*.csv
*.parquet

# figures are regenerated from results JSON
*.png
