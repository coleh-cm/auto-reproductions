# Reproduction: CASteer: Cross-Attention Steering for Controllable Concept Erasure

- **Paper:** CASteer: Cross-Attention Steering for Controllable Concept Erasure
- **Authors:** Tatiana Gaintseva, Andreea-Maria Oncescu, Chengcheng Ma, Ziquan Liu, Martin Benning, Gregory Slabaugh, Jiankang Deng, Ismail Elezi
- **Year:** 2026
- **arXiv:** 2503.09630
- **Date started:** 2026-08-17
- **paper_ref:** d0ab97b5-e9bf-41af-9846-d19f846eef80
- **project_id:** 06910d54-1d99-4864-8bff-ab3007a0c70e

## Status

**In progress — implementation + correctness complete; diffusion-number claims BLOCKED on CPU.**

- [x] Reproduction folder created; branch `repro/casteer-cross-attention-steering-for-controllable-concept-erasure` pushed.
- [x] arXiv e-print 2503.09630 fetched and unpacked under `paper/` — `.tex`/`.bib` committed, figure images and style files gitignored (no `.bbl` present in the source tarball).
- [x] Rendered HTML saved as `paper/paper.html` and committed.
- [x] SPEC.md — method written up as an algorithm with symbol shapes and cited equations; unstated-items analysis (U1–U14, U14 = LPIPS_e direction contradiction); arms, restrictions, 17-claim `claims.json`; §11 Constructed truth, §12 unstated-value sweeps, §13 environment constraint.
- [x] claims.json validator round 1 fixed: `arms` (9) + `arm_configs` declared; per-arm `restrictions` map (all `narrows_situations`); curve claims carry `x` + `against`; every `sensitivity` numeric `plausible`/`survives` with non-degenerate survival ratio (i2p claim reframed to images-per-prompt with full-width survival + inconclusive-below-3000 note); Snoopy CS claims moved to the paper's own per-seed normalized CS (`experiments.tex:75`) so the CLIP-checkpoint sensitivity is honestly survivable across [512,768].
- [x] Upstream code search — **official code exists**: `abstract.tex:36` → https://github.com/Atmyre/CASteer (pinned at `135912a555a8606c01f55843c738cf8062319ed7`); vendored verbatim at repo root (commit `6f3b97f`).
- [x] Implementation / environment setup — `requirements.txt`, `Dockerfile`, `README.md`, `pyproject.toml`, `.gitignore`, import gate `tests/test_environment.py`.
- [x] **Method core implemented** (`core/controller.py`): added constant-α Eq.4 mode (`steering_mode='constant'`, SPEC U13) for the `const_a*` arms; added Eq.9 multi-concept average (`average_concept_vectors`, no re-normalization, U9); fixed a CPU bug where `forward` hardcoded `vector.half()` (now preserves the caller's dtype — `float16` on GPU, `float32/float64` on CPU); kept the dot-product Eq.6/7 path numerically identical for `float16`. `tests/test_method_core.py` (8 invariants) + `tests/test_degeneracy.py` (β=0 bit-exact identity on both modes, inactive-control identity) pass.
- [x] **Data pipeline implemented** (`core/data.py`): fingerprinted loaders for ImageNet classes (50, `tench`), CLIP templates (80), COCO captions (30,000), I2P prompts (4,703 via `AIML-TUDA/i2p`), and the real COCO-30k FID reference (RAISES if not vendored — never substitutes synthetic). `tests/test_data.py` passes (incl. live I2P fingerprint).
- [x] **Evaluation metrics implemented** (`core/eval/metrics.py` + device-agnostic `core/eval/clip.py`): CLIP score (ViT-B/32, CPU-runnable), FID (clean-fid, `device='cpu'`), and NudeNet/Q16/LPIPS which RAISE `MissingEvaluatorError` when their tool is not installed. Fixed two real bugs in `core/eval/clip.py`: hardcoded `device="cuda"`/`.cuda()` (crashed on CPU) and the `range(len(images)//50)` loop that silently dropped the trailing <50 images. `tests/test_eval.py` (11 instrument tests) passes.
- [x] **Runner implemented** (`core/runner.py`, `scripts/diffusion/run_all_arms.py`, `scripts/diffusion/smoke.py`): CPU-friendly pipeline loader (fp32 on CPU, fp16 on GPU, no `device_map`); per-arm config mirrored from `claims.json`; `is_arm_feasible` drives the BLOCKED decision on CPU; fail-loud-on-empty everywhere.
- [x] **smoke runs end-to-end on CPU**: `smoke.sh` loads SD-1.4 (fp32), estimates 80 steering vectors from 2 prompt pairs (Algorithm 1, shared seed 0, patch-mean, pair-mean, pos−neg, unit-norm), generates 1 steered (casteer_clip, β=2, Eq.7 clip) + 1 vanilla image at 4 steps/256²/seed 42, then scores the steered image with the paper's `snoopy_cs` metric (mean CLIP score on a Snoopy prompt, ViT-B/32) via the same eval code path the full arms use. Prints exactly one stdout line `FINAL casteer_clip=<snoopy_cs>` (arm name from claims.json, real numeric measurement; provenance metadata to stderr), ~41 s once SD-1.4 is cached. NOT evidence about the paper.
- [x] **Degeneracy + invariants tests + mutations**: 65 tests pass; `mutations.json` (16 deliberate defects) each caught by its `must_fail` test (verified by applying find→replace and watching the named node fail; 0 surviving). `instruments.json` records every correctness-deciding instrument with positive/negative tests.
- [x] **`selfcheck_claims.py` → `selfcheck.json`**: `house` PASS (max norm error 3.5e-14, unit-norm OK); 16 diffusion claims BLOCKED. This is our own evaluator; `claims_result.json` is produced by the workflow's numbers gate, not by us.
- [x] **`measured.json`**: all diffusion metrics `BLOCKED` with `measured_blocked_reasons.json` sidecar (CPU-only host; paper full config infeasible without GPU, `supplementary.tex:30`). The pure-math `house` invariant is NOT blocked: `core/invariants.py::compute_house_invariant` fills `house_max_norm_err` / `house_unit_norm_ok` / `house_pass` under arm `casteer_noclip` at every seed (CPU-runnable, no generation), so the gate can settle claim `house` to `reproduced` rather than `unevaluable`.
- [x] **Claim expressions are valid Python (gate-evaluable).** The prior gate run reported 15 claims `unevaluable` with `SyntaxError` because each claim's `quantity`/`against`/`predicate` carried trailing prose (`(18 = SAeUron ...)`, `\textit{Snoopy}`, commas inside parentheticals) that is not valid Python. Fixed: every `quantity`/`against`/`predicate` is now a pure Python expression referencing `measured.<arm>.<metric>` and numeric constants (`max`/`min` builtins only); the derivation prose moved to a `note` field per claim (traceability preserved, eval unaffected). Verified by compiling and eval'ing every expression against a dummy namespace — 0 syntax errors. Re-running `run_all_arms.sh` regenerated `measured.json`; a local re-simulation of the gate's per-seed evaluation now yields `house` → `reproduced` (held at 3 of 3 seeds) and the 16 diffusion-number claims → `blocked` (every referenced value `BLOCKED`). No claim is `unevaluable`.
- [ ] Adversarial review loop clean (the `orchestrate` tool returned Internal Server Error on every call in this sandbox, including trivial probes; components were built directly with self-review against the paper and verified by tests + mutations — see "Orchestration" note below).
- [ ] Readiness gates.
- [ ] Publish.

## What this run reproduces

- **`house` (the Householder norm-preservation invariant)** — fully reproduced (pure math, no generation). High compute-invariance.
- **The method core is correct and tested**: Eq.6 (matrix == dot-product), Eq.7 (clip only positive projections), Eq.4 (constant α), Eq.9 (multi-concept average, not re-normalized), degeneracy at β=0 (bit-exact baseline), CFG conditional-half-only steering, unit-norm construction (U1).
- **The code path runs end-to-end** (smoke): estimate → steer → generate, on the real SD-1.4, on CPU.

## What this run CANNOT reproduce (BLOCKED)

- All 16 diffusion-number claims. This sandbox is CPU-only; the paper's full config (50 steps × ≥1,000 prompts × 3 seeds × multiple arms, on 8×V100, `supplementary.tex:30`) is infeasible on CPU (weeks of wall time). `measured.json` records `BLOCKED` for every diffusion metric; `run_all_arms.sh` emits `FINAL <arm>=BLOCKED` per arm/seed. The external evaluators (NudeNet, Q16, LPIPS) are not installed and the real COCO-30k FID reference is not vendored — both are documented blockers, not synthetic substitutes.

## Orchestration

The workflow's implementation step was to call `orchestrate` to build the 5 components in parallel and review each against the paper. The `orchestrate` tool returned **Internal Server Error on every call** in this sandbox on the prior pass, including a trivial `META + return {}` probe and a one-agent `agent('say pong')` probe. The tool is unavailable here. The components were therefore built directly, with self-review against the paper (`paper/content/*.tex`, cited in SPEC.md), and verified objectively by the test suite (53 passing), the mutation suite (9/9 caught), and the live smoke run. This deviation is recorded here for provenance.

## Curve-claim figures

The two curve claims (`fig_clip_shape`, `fig_fid_shape`) come from the paper's figures `paper/content/images/snoopy_clip_vs_clip_2.png` and `snoopy_clip_vs_fid_2.png` (axes read in `figure_transcript.md`: normalized Snoopy CS vs mean other-concepts CS / vs mean other-concepts FID). The paper's figure PNGs are gitignored under `paper/.gitignore` (style/asset policy) but remain on disk for a reader to inspect. **These figures cannot be regenerated here**: each plots measured CS/FID points for the steered arms, which are BLOCKED on this CPU-only host (§ above). Per the reproduction rule, a regenerated figure + the paper's figure are a pair for a reader to compare and are **not** evidence — the gate's verdicts (in `claims_result.json`, produced by the numbers gate) are the evidence. We did not fabricate a figure from BLOCKED numbers; the curve claims stand at BLOCKED with the declared `against` constants recorded in `claims.json` (SPM/SAFREE/DoCo norm-CS = [0.7758, 0.6969, 0.6255]; Receler/ESD mean-other-FID = [77.5, 78.88, 106.64]) so a GPU run can complete the comparison. The axis ranges/units the figures use: x = normalized Snoopy CS ∈ ~[0.5, 0.85] (paper's own per-seed normalization, `experiments.tex:75`); y = mean CLIP score of other concepts (fig 4) / mean FID of other concepts (fig 5).

## Figure readings

- `figure_transcript.md` — `read-figure` Q&A on the two Snoopy scatter plots (`snoopy_clip_vs_clip_2.png`, `snoopy_clip_vs_fid_2.png`). Note: first two calls returned empty answers under a too-small `--max-tokens` (reasoning budget consumed); re-asked with `--max-tokens 2000`. Findings: Ours is left of SPM/SAFREE/DoCo on both plots; above Receler/ESD on clip-vs-clip; below Receler/ESD on clip-vs-fid; Ours ≈ (0.59, 0.983) — consistent with table-derived (0.583, 0.982). Emitted as curve claims `fig_clip_shape` / `fig_fid_shape`.

## Source layout

- `paper/paper.html` — reading surface (MathML rendering).
- `paper/iclr2026_conference.tex` — main file; `paper/math_commands.tex` — preamble macros (resolve these before quoting equations).
- `paper/content/*.tex` — abstract, intro, method (`method_2.tex`), experiments, related work, conclusion, supplementary.
- `paper/content/<model>_tables/*.tex` — results tables per model (SD 1.4, SD 1.4 step-0, SD 1.5, SDXL, Sana, multi-concept, strength, UNet-padding, constant-strength ablations).
- Quotes use `<file on disk>:<line>` citations against this `.tex` source.

## Log

- 2026-08-17: Repo cloned shallow/blobless. Branch `repro/casteer-cross-attention-steering-for-controllable-concept-erasure` created and pushed. Both arXiv copies fetched successfully (e-print tarball + HTML); no fetch failures to record.
- 2026-08-17: Read full paper (method, experiments, all tables, supplementary + algorithms). Inspected upstream `core/{controller,diffusion_steering,vector_dump,utils,construct_prompts}.py` + `estimate_steering_vectors.py` to pin interfaces and confirm ambiguity resolutions (unit-norm f_norm; attn2-module output hook; conditional-CFG-branch-only steering; SD-1.4 at 50 steps/gs-default/512²; shared global seed 0 for vector construction). Wrote `SPEC.md` + `claims.json` (17 claims, 10 high-compute-invariance) and committed figure transcripts. Key internal inconsistency recorded as U1: paper prints `f_norm(v)=v/||v||²` (method_2.tex:81, supplementary.tex:60) but its own projection-length (method_2.tex:125) and Householder (experiments.tex:21-22) statements plus upstream code all require unit norm — adopted unit norm, flagged for the sweep.
- 2026-08-17: instruments.json fix pass. The gate flagged `positive_test`/`negative_test` entries that were prose rather than `<file>::<test>` node references (fid_scorer, degeneracy_equivalence_check, householder_invariant_check, nudenet_detector, q16_classifier, lpips_scorer). Rewrote every entry as a JSON array of real pytest nodes. Added two missing negative-test nodes that prove the checks *reject* a known-wrong input: `tests/test_degeneracy.py::test_degeneracy_rejects_nonzero_offset` (the bit-exact check rejects a `+0.001` offset at β=0, the `degeneracy_offset` defect) and `tests/test_method_core.py::test_householder_rejects_half_strength` (the norm-preservation invariant rejects `(I - 1·s sᵀ)c`, the `half_strength_projection` defect). For the not-installed graders (NudeNet/Q16/LPIPS) the `positive_test` is the raises-when-unavailable node and the `negative_test` is `test_metrics_no_ok_on_empty_dirs` (no OK-on-empty). Full suite: 35 passed.
- 2026-08-17: **Mutation-survivor fix pass.** The gate reported two mutations that SURVIVED (their `must_fail` test passed on the known-wrong implementation, so the test was not evidence):
  1. `half_strength_projection` (`core/controller.py`, `res = 0.5 * strength * (v @ pinv(v))`) survived `test_eq6_noclip_matches_matrix_form`. Root cause: the test exercised only the `steering_mode='dotproduct'` path, which routes to `steer_with_clipping` — and that path reads `self.strength` directly, NEVER the precomputed projection matrix `P`. The mutation only changes `P`, so the test was blind to it. Worse, `P` itself was already broken: `P = torch.eye(res.shape[1]) - res`, but for the production store shape `[num_heads, 1, d]` the `@` yields a 4-D `res` `[num_heads, 1, d, d]`, so `res.shape[1]` is the batch dim (==1) and `torch.eye(1)` broadcasts to an all-ones matrix — `P = J - β s sᵀ` instead of `I - β s sᵀ`. This was latent (the matrix form `steer_matrix_form` is dead code; production arms use `steer_with_clipping`, which uses the steering vector `b` directly and is unaffected, so `measured.json` is unchanged). **Fix:** (a) `core/controller.py`: size the identity by the hidden dim, `torch.eye(res.shape[-1])`, so `P = I - β s sᵀ` for any store shape; (b) `tests/test_method_core.py::test_eq6_noclip_matches_matrix_form`: now ALSO calls `steer_matrix_form` (the consumer of `P`) and asserts it equals `(I - 2 s sᵀ) c` and preserves `‖c‖` — the mutation drives `P` to `I - 1 s sᵀ`, which the matrix-form check fails (output diverges by ~0.2, norm by ~0.03, far above the 1e-4 tolerance). The elementwise (Eq.6) check is kept.
  2. `no_ok_on_empty_disabled` (`core/eval/metrics.py`, `_list_images` returns `[]` instead of raising on an empty dir) survived `test_metrics_no_ok_on_empty_dirs`. Root cause: the test only checked the *downstream* `nudity_total` / `q16_inappropriate_count`, which raise `MissingEvaluatorError` when their backing tool is absent (nudenet / the Q16 checkpoint are not installed here) — so they raise regardless of whether `_list_images` raised, and the mutation slipped through. **Fix:** `tests/test_eval.py::test_metrics_no_ok_on_empty_dirs` now DIRECTLY asserts `M._list_images(str(empty))` raises `ValueError` (the exact line the mutation removes — under it the function returns `[]`, so `pytest.raises(ValueError)` fails), plus a `FileNotFoundError` check for a non-existent dir; the downstream checks are kept as belt-and-suspenders.
  Re-ran the full mutation harness: all 6 `must_fail` nodes now FAIL on the mutated implementation (0 surviving); the full suite is 35 passed on the correct implementation. `core/controller.py` change touches only the dead `P`/`steer_matrix_form` path (production arms route through `steer_with_clipping`/`steer_constant`, which read the steering vector `b` directly), so `measured.json` is unchanged.
- 2026-08-17: **Unevaluable-claims fix pass.** The gate returned 15/17 claims `unevaluable` with `SyntaxError` (and 2 `blocked`). Root cause: the numbers gate evaluates each claim's `quantity`/`against`/`predicate` as a Python expression over `measured.<arm>.<metric>`, but every one of those 15 fields carried trailing prose (`(18 = SAeUron, best prior, sd14_tables/nudity.tex:28; paper value 7, nudity.tex:30)`, `\textit{Snoopy}`, parentheticals with commas, etc.) that is not valid Python — `eval` raised `SyntaxError: invalid syntax` / `expression cannot contain assignment` / `unterminated string literal`. (The 2 already-`blocked` claims had clean expressions and were unaffected.) Fix, per the feedback ("a claim that cannot be evaluated is a defect in the reproduction: measure what the predicate needs, mark the arm BLOCKED, or fix the claim"): (1) **Fixed the claims** — every `quantity`/`against`/`predicate` is now a pure Python expression referencing `measured.<arm>.<metric>` and numeric constants (`max`/`min` builtins only); the derivation prose moved to a new `note` field on each claim, so a reviewer can still trace the arithmetic but `eval` is unaffected. Verified by compiling + eval'ing all 17 expressions against a dummy `measured` namespace: 0 syntax errors. (2) **Measured what `house` needs** — `house` is pure maths (no GPU, no generation), so the prior "BLOCKED" was itself wrong: added `core/invariants.py::compute_house_invariant` (deterministic per-seed: 100 random unit s × random c in dims {320,640,1280}, float64; max |‖(I−2 s sᵀ)c‖−‖c‖| and a unit-norm construction check) and wired it into `scripts/diffusion/run_all_arms.py` so the `casteer_noclip` arm gets real `house_max_norm_err`/`house_unit_norm_ok`/`house_pass` values at every seed EVEN while its diffusion metrics stay `BLOCKED`. Added `tests/test_invariants.py` (positive: holds at seeds 42/1234/2024, deterministic, correct arm/metric names; negative: a β=1 non-Householder reflection is rejected) and registered a `householder_invariant_scorer` instrument in `instruments.json`. Regenerated `measured.json` (`run_all_arms.sh`): `casteer_noclip.<seed>.house_pass = 1`, `house_max_norm_err ≈ 3.5e-14` at all 3 seeds; all diffusion metrics still `BLOCKED`. A local re-simulation of the gate's per-seed evaluation now yields `house` → `reproduced` (held at 3 of 3 seeds) and the 16 diffusion-number claims → `blocked` (every referenced value `BLOCKED`); **0 claims `unevaluable`**. Full suite: 39 passed (35 prior + 4 new invariants tests). `claims_result.json` is NOT touched by us — the workflow's numbers gate produces it; the version on disk from the prior pass (15 unevaluable / 2 blocked) is stale and will be overwritten by the next gate run.


- 2026-08-17: **Review-driven fix pass (F1/F2/F3 + A1/A3 + minor latent).** The adversarial review found three latent defects that would corrupt the exact headline claims if the GPU path were completed, plus two paper-definition divergences in runnable paths. All five are fixed, covered by new tests, and covered by new mutations (9/9 caught, 0 surviving; 53 tests pass):
  - **F1 NudeNet label set (fixed):** `core/eval/metrics.py::nudity_total` now counts ONLY the 8 EXPOSED NudeNet classes the paper's table sums (`NUDENET_EXPOSED_CLASSES`, `nudity.tex:9`), filtering out face and `*_COVERED` detections. Verified: 183+21+46+10+44+42+171+129=646=SD-1.4 Total. The prior code counted every detection >=0.6 over all classes, which would have inflated the total and falsely refuted the nudity claims. Tests: `test_nudenet_exposed_classes_are_exactly_eight`, `test_nudenet_label_filter_counts_only_exposed` (mock detector). Mutation: `nudenet_count_all_classes`.
  - **F3 per-task vector mapping (fixed):** `core/runner.py` selects the steering vector per TASK via `TASK_VECTOR` (snoopy->Snoopy, style->Van Gogh, i2p->nudity, i2p_overall->7-class average, coco->nudity), not per arm. The prior `vector="nudity"` on every steered SD-1.4 arm would have steered snoopy/style runs with the nudity vector and measured the wrong quantity. The 7-concept Eq.9 average for the I2P-overall task is composed on the fly from the 7 per-concept stores (`_resolve_task_vector` + `average_concept_vectors`); previously `average_concept_vectors` had no production caller. Tests: `tests/test_runner.py` (10 tests). Mutation: `snoopy_task_uses_nudity_vector`.
  - **F2 structural dead end (fixed):** `scripts/diffusion/run_all_arms.py::_run_and_evaluate` is now a real arm->eval->measured.json driver, not an unconditional `MissingEvaluatorError` stub. Each metric is scored independently (a missing NudeNet marks `nudity_total` BLOCKED but lets `snoopy_cs` still compute). The README's "on a CUDA host the same script runs them for real" is now true. Two metrics remain BLOCKED on every host until their references are supplied (Q16 checkpoint, COCO-30k reference) — honest structural blocks, documented in `measured_blocked_reasons.json`.
  - **A1 SDXL guidance (fixed):** `core/runner.py` uses `guidance_scale=5.0` for the `sdxl` arms (diffusers `StableDiffusionXLPipeline` default; `supplementary.tex:255` "All other parameters are left default"), `7.5` for `sd14` (diffusers default, SPEC U4), `0.0` for `sdxl-turbo`. The prior forced 7.5 for SDXL was NOT the default and would have quietly raised CLIP score / altered FID for the sdxl arms.
  - **A3/U9 Eq.9 re-normalization (fixed):** `core/controller.py::steer_with_clipping` and `steer_constant` now use the stored steering vector `b` AS-IS, NOT `b/||b||`. For single-concept (unit) stores this is a no-op (all existing tests pass unchanged); for the Eq.9 multi-concept average `b` is the sub-unit mean, and using it as-is applies `(I - beta * b b^T) c` with the sub-unit `b` — the paper-literal "simply averaging" (`supplementary.tex:1068-1071`), not the renormalized form that would multiply effective suppression by ~1/||mean|| (~2.6x for 7 concepts). This also makes the dot-product path consistent with the matrix form `steer_matrix_form`. Tests: `test_multi_concept_no_renormalize_at_steer`, `test_multi_concept_no_renormalize_constant_mode`. Mutation: `renormalize_multiconcept_mean_at_steer`.
  - Minor latent fixes in the same pass: LPIPS now uses `normalize=True` for [0,1] ToTensor inputs (the package expects [-1,1]); the COCO caption loader no longer carries the false `horse`-filter justification (the 5 other Snoopy concepts are Mickey/Spongebob/Pikachu/dog/legislator, `experiments.tex:64`); the stale `.half()` comment in `tests/test_environment.py` is corrected. The vendored `scripts/diffusion/run_i2p_eval.py:49` still hardcodes `use_first_diffusion_step=True` (upstream-identical; runs the appendix single-step ablation) — left as vendored but the README's hand-invocation now documents the `--use_all_diffusion_steps` requirement for SD-1.4.
  - `measured.json` regenerated: unchanged shape (house values identical: 3.55e-14 / 3.55e-14 / 2.84e-14 at seeds 42/1234/2024; 16 diffusion metrics BLOCKED with honest reasons). `claims_result.json` (the workflow's verdict table) unchanged: house `reproduced`, 16 `blocked`.

- 2026-08-17: **Second adversarial-review fix pass (driver + eval-path corrections).** Three reviewers (faithful / metric / divergence) converged: the equation-level implementation is paper-faithful, but the GPU driver would silently mis-settle or structurally block the paper's headline claims, and several number-moving divergences from the paper's measurement protocol were undocumented. The CPU-only BLOCKED verdicts were honest, but 6 of them were blocked for *structural* reasons mislabeled as "CPU-only". All fixed, covered by new pure-function tests + 8 new mutations (16/16 caught, 0 surviving; 65 tests pass). The `orchestrate` tool returned Internal Server Error again in this sandbox (trivial probe), so review was self-review against the paper (file:line), verified objectively by tests + mutations — same approach as the prior pass.
  - **nudity_total full-set scaling (fixed):** `core/eval/metrics.py::scale_count_to_full_set` + `nudity_scaled_or_inconclusive` put the subset count on the full-set basis (4,703) so it is commensurate with the paper's constants (18/7/646); the RAW count is recorded under `nudity_total_raw` (claims.json `metrics.nudity_total_raw`). Below the declared 2000-prompt floor the driver reports BLOCKED (inconclusive) instead of a ~4.7×-deflated number. `FULL_CONFIG_MIN_IMAGES["i2p"]` is now 4,703 (paper full set). The prior driver stored the raw subset count and would compare it against full-set constants — the exact subset-bias failure this review exists to catch. Tests: `test_scale_count_to_full_set`, `test_nudity_scaled_or_inconclusive_*`. Mutations: `scale_count_returns_raw`, `nudity_skip_inconclusive_floor`.
  - **Structurally-unsettleable claims made reachable (fixed):** the sd14 normalization reference for snoopy/other/style is now generated ON DEMAND per steered arm at the same seed (lazily cached) and scored in-memory; `norm_snoopy_cs` is lifted out of the `snoopy_cs` guard; `mean_norm_others_cs`/`mean_others_fid` use the in-memory sd14 per-concept references. The dead `_sd14_metric` (read stale `measured.json`) and `_sd14_others_cs` (unconditional `return None`) are removed. `arm_metrics_from_claims` now parses `predicate` (house's predicate-only metrics register). On a GPU host the 6 normalized-Snoopy claims are reachable; on CPU they are BLOCKED for the honest CPU reason (no longer a structural block). Tests: `test_arm_metrics_from_claims_*`. Mutation: `arm_metrics_skip_predicate`.
  - **Declared image counts via n_per (fixed):** `core/runner.py::expand_template_prompts` repeats each template `ceil(n_target/len(templates))` times so snoopy/other reach 800/concept (80×10, experiments.tex:67) and style reaches 200 (50×4), not 80/50 (the dead `n_per` left them below the >=200 restriction). Tests: `test_expand_template_prompts_*`. Mutation: `n_per_not_expanded`.
  - **I2P per-prompt sd_seed (fixed):** `core/data.py::load_i2p_seeds` exposes the curated per-prompt `sd_seed` (vendored run_i2p_eval.py:71 — the protocol that produced the SD-1.4 Total=646 anchor); `run_generation` takes `prompt_seeds` and seeds a fresh generator per I2P prompt. The prior continuous-generator stream was an undocumented divergence that plausibly deflated nudity counts. Tests: `test_i2p_seeds_fingerprint`.
  - **CLIP bare-concept reference text (fixed):** `core/eval/metrics.py::cs_reference_prompts` returns `[concept]*n` (paper pipeline: produce_scores.py:43 -> clip.py:191), not the full filled template. Tests: `test_cs_reference_prompts_is_bare_concept`. Mutation: `cs_reference_full_template`.
  - **Eq.9 renormalization fork recorded (fixed):** the as-is (no renorm) reading is text-literal and conservative, but the paper's I2P number (25.58) was produced by the renormalized upstream path. The fork is now in `claims.json::i2p_overall_beats_receler.committed_readings` and SPEC §12 with a surviving range of {as_is} (zero width) — a reader sees the verdict depends on a reading the paper's numbers were NOT produced under. The 7-concept string is corrected to `illegal activity` (supplementary.tex:1071; the prior `illegal` matched neither paper variant). The controller docstring is corrected: the dot-product (as-is) and matrix (`s s⁺`, renormalizing) paths AGREE only for unit `b` and DIVERGE for the Eq.9 sub-unit mean. Tests: `test_i2p_overall_is_the_seven_concept_average`. Mutation: `i2p_overall_concept_illegal`.
  - **Per-model steering-vector store (fixed):** `build_controller` loads vectors from `<vector_dir>/<vector_model>` (sd14 -> `sd14`, sdxl_casteer_clip -> `sdxl-turbo`), so sdxl_casteer_clip cannot silently load SD-1.4's `nudity.pt` (dim-mismatch / wrong-model steering). Tests: `test_build_controller_per_model_vector_dir_isolates_sdxl_from_sd14`, `test_arm_config_vector_model_is_set_for_steered_arms`. Mutation: `vector_dir_not_per_model`.
  - **Sensitivity self-consistency + hygiene (fixed):** `nudity_two_times_fewer.sensitivity.survives` is [1] (at k=2 the count doubles, 2×14−18=10>0 fails), not the self-inconsistent [1,2]; `nudity_beats_all_prior` keeps [1,2]. The LPIPS 256×256-resize comment is corrected (a REAL transform, not a no-op; recorded in the style claim's sensitivity). Duplicate `direction` keys in two claims removed.
  - `measured.json` regenerated: unchanged shape (house values identical; 16 diffusion metrics BLOCKED). The BLOCKED reason text now reflects the corrected full config (I2P=4,703 / snoopy+other=800 / COCO=3,000). `claims_result.json` (the workflow's verdict table) is produced by the numbers gate, not by us.
