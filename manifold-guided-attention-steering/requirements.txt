# requirements.txt - Manifold-Guided Attention Steering (MAGS) reproduction
# Python 3.13. Fully pinned (direct + transitive) for bit-for-bit reproducibility.
# Resolved & import-verified on cpython-3.13 / linux-aarch64; same pins resolve on x86_64.

# === Direct dependencies the method needs ===
accelerate==1.14.0   # device_map loading for 8B-20B models
datasets==5.0.1   # HuggingFace datasets - MATH-500, GSM8K, MBPP, APPS, MathInstruct
huggingface-hub==1.25.1   # model/dataset download + revision SHAs (transitive, pinned). Bumped (was 0.36.2): required by transformers==5.14.1, the FIRST release whose AutoModel registry has Gemma4ForConditionalGeneration (google/gemma-4-E4B-it) AND GptOssForCausalLM (openai/gpt-oss-20b); the prior pin 4.57.1 has neither class, so the reproduction could not load 2 of the 3 paper model families even on a GPU host (round-23 fix).
human-eval==1.0.3   # HumanEval pass@1 evaluation harness
latex2sympy2-extended==1.11.0   # math_verify backend (transitive, pinned)
math-verify==0.9.0   # boxed/number answer grading for MATH-500 and GSM8K
matplotlib==3.11.1   # latent-trajectory (Fig.4) and attention-shift (Fig.5) figure reproduction
numpy==2.5.1   # .npz activation/manifold storage; array math
pillow==12.3.0   # rdkit image backend (transitive, pinned)
pytest==9.1.1   # test runner (required by repro gate)
rdkit==2026.3.4   # SMILES validity for molecular generation (Table 3 validity metric)
safetensors==0.8.0   # model weight loading
scikit-learn==1.9.0   # roc_auc_score (head AUROC), LogisticRegression (ITI probes), PCA
scipy==1.18.0   # scikit-learn backend; percentile/qr helpers (transitive, pinned)
sentencepiece==0.2.2   # Llama/Gemma tokenizer backend
sympy==1.14.0   # math_verify backend (transitive, pinned)
tokenizers==0.22.2   # HF fast tokenizers (transitive of transformers, pinned)
torch==2.7.1   # PyTorch - tensors, SVD (Eq.5), forward passes, o_proj hooks
transformers==5.14.1   # HuggingFace Transformers - load Llama-3.1-8B-Instruct, Gemma-4-E4B-it, GPT-OSS-20b; chat templates; generation. Pinned to 5.14.1 (was 4.57.1): 4.57.1 has NO Gemma4ForConditionalGeneration and NO GptOssForCausalLM, so 2 of the 3 paper model families were unloadable on ANY host; 5.14.1 is the first release with both. Verified: tests/test_gemma4_adapter.py builds the real google/gemma-4-E4B-it on the meta device (config-only, no weights, no GPU) and the MAGS hook resolves + reshapes on the real architecture (round-19 wrote this adapter against 5.14.1 but the lock was never bumped, so the 4 tests silently SKIPPED for 18 rounds).

# === Pinned transitive dependencies (lock the resolved set) ===
aiohappyeyeballs==2.7.1
aiohttp==3.14.3
aiosignal==1.4.0
annotated-doc==0.0.5   # NEW (transformers 5.x): typer CLI arg docs
antlr4-python3-runtime==4.13.2
anyio==4.14.2
attrs==26.1.0
certifi==2026.7.22
charset-normalizer==3.4.9
click==8.4.2   # NEW (transformers 5.x): typer CLI backend
contourpy==1.3.3
cycler==0.12.1
dill==0.4.1
filelock==3.32.0
fire==0.7.1
fonttools==4.63.0
frozenlist==1.8.0
fsspec==2026.6.0
h11==0.16.0
hf-xet==1.5.2
httpcore==1.0.9
httpx==0.28.1
idna==3.18
iniconfig==2.3.0
jinja2==3.1.6
joblib==1.5.3
kiwisolver==1.5.0
markdown-it-py==4.2.0   # NEW (transformers 5.x): rich CLI rendering
markupsafe==3.0.3
mdurl==0.1.2   # NEW (transformers 5.x): markdown-it-py dep
mpmath==1.3.0
multidict==6.7.1
multiprocess==0.70.19
narwhals==2.24.0
networkx==3.6.1
packaging==26.2
pandas==3.0.5
pluggy==1.6.0
propcache==0.5.2
psutil==7.2.2
pyarrow==25.0.0
pygments==2.20.0
pyparsing==3.3.2
python-dateutil==2.9.0.post0
pyyaml==6.0.3
regex==2026.7.19
requests==2.34.2
rich==15.0.0   # NEW (transformers 5.x): typer CLI rendering
setuptools==83.0.0
shellingham==1.5.4   # NEW (transformers 5.x): typer shell detection
six==1.17.0
termcolor==3.3.0
threadpoolctl==3.6.0
tqdm==4.70.0
typer==0.27.0   # NEW (transformers 5.x): `transformers` CLI
typing-extensions==4.16.0
urllib3==2.7.0
xxhash==3.8.1
yarl==1.24.5
